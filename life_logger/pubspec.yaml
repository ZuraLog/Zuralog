name: life_logger
description: "Life Logger — AI Health Assistant Edge Agent"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: any
  riverpod_annotation: any

  # Navigation
  go_router: any

  # Networking
  dio: any
  web_socket_channel: any

  # Local Storage
  drift: any
  sqlite3_flutter_libs: any
  flutter_secure_storage: any
  path_provider: any

  # Utilities
  url_launcher: any
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: any
  riverpod_generator: any
  drift_dev: any

flutter:
  uses-material-design: true
