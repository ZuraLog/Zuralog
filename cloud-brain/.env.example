# =============================================================================
# Life Logger Cloud Brain — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# =============================================================================

# --- Database (Local: Docker Compose / Production: Supabase) ---
DATABASE_URL=postgresql+asyncpg://lifelogger:lifelogger@localhost:5432/lifelogger

# --- Redis (Local: Docker Compose / Production: Upstash) ---
REDIS_URL=redis://localhost:6379/0

# --- Supabase (Used for Auth + RLS in production) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# --- Application ---
APP_ENV=development
APP_DEBUG=true
