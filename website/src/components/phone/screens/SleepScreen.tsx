// website/src/components/phone/screens/SleepScreen.tsx
"use client";

/**
 * Placeholder for the Sleep screen shown inside the phone mockup.
 * Final design: sleep score, sleep stages chart (REM / Deep / Light), sleep duration.
 */
export function SleepScreen() {
  return (
    <div className="w-full h-full flex items-center justify-center bg-[#F0EEE9]">
      <span
        className="text-xs text-[#9B9894]"
        style={{ fontFamily: "var(--font-jakarta)" }}
      >
        Sleep
      </span>
    </div>
  );
}
