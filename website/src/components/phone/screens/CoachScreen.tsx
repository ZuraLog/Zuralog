// website/src/components/phone/screens/CoachScreen.tsx
"use client";

/**
 * Placeholder for the Coach screen shown inside the phone mockup.
 * Final design: scripted AI conversation — user message types out character by character,
 * Zura's response streams in word by word.
 */
export function CoachScreen() {
  return (
    <div className="w-full h-full flex items-center justify-center bg-[#161618]">
      <span
        className="text-xs text-[#9B9894]"
        style={{ fontFamily: "var(--font-jakarta)" }}
      >
        Coach
      </span>
    </div>
  );
}
