export function ProgressSection() {
    return (
        <section
            id="progress-section"
            className="relative w-full bg-ds-canvas"
            style={{ height: '100vh' }}
        />
    );
}
