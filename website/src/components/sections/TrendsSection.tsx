export function TrendsSection() {
    return (
        <section
            id="trends-section"
            className="relative w-full bg-ds-canvas"
            style={{ height: '100vh' }}
        />
    );
}
